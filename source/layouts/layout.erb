<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>
      <% unless(current_page.url == "/") %>
        <%= current_page.data.title %>
        <% if(current_page.url =~ %r{/docs/\w+/?$}) %>
          APIs
        <% elsif(current_page.url =~ %r{/docs/\w+/.+}) %>
          API
        <% end %>
        |
      <% end %>
      NLR: Developer Network
    </title>

    <link rel="canonical" href="https://developer.nrel.gov<%= current_page.url %>" />

    <%= vite_stylesheet_tag("all.scss") %>
    <%= partial("layouts/analytics") %>
    <%= vite_javascript_tag("all") %>

    <link rel="shortcut icon" type="image/x-icon" href="<%= vite_asset_path "nrel-app-template/img/favicon.ico" %>">
    <link rel="apple-touch-icon" href="<%= vite_asset_path "nrel-app-template/img/touch-icon-iphone.png" %>">
    <link rel="apple-touch-icon" sizes="72x72" href="<%= vite_asset_path "nrel-app-template/img/touch-icon-ipad.png" %>">
    <link rel="apple-touch-icon" sizes="114x114" href="<%= vite_asset_path "nrel-app-template/img/touch-icon-iphone-retina.png" %>">
    <link rel="apple-touch-icon" sizes="144x144" href="<%= vite_asset_path "nrel-app-template/img/touch-icon-ipad-retina.png" %>">
  </head>
  <body class="<%= page_classes %>">
    <%= partial("layouts/noscript_analytics") %>
    <%= partial("layouts/header") %>
    <main id="content" class="container-xl">
      <%= partial("layouts/breadcrumbs") %>

      <%= yield %>

      <%= partial("layouts/edit_me") %>
    </main>
    <%= partial("layouts/footer") %>
  </body>
</html>
