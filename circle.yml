deployment:
  production:
    branch: master
    commands:
      - git config user.email "noreply-circleci@nrel.gov"
      - git config user.name "CircleCI Build Bot"
      - bundle exec rake publish
