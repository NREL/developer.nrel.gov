machine:
  environment:
    GIT_AUTHOR_NAME: CircleCI Build Bot
    GIT_AUTHOR_EMAIL: noreply@circleci.com

deployment:
  production:
    branch: master
    commands:
      - bundle exec rake publish
